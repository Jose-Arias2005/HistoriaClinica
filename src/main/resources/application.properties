# Nombre de la aplicación
spring.application.name=demo

# Configuración de la base de datos MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/base_datos?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC

spring.datasource.username=root
spring.datasource.password=MySQL
server.port=9090


# Proveedor de JPA (Hibernate)
spring.jpa.hibernate.ddl-auto=update

# Mostrar consultas SQL en la consola (opcional para depuración)
spring.jpa.show-sql=true

# Dialecto de Hibernate para MySQL
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect



